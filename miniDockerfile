# Use a base that suits your app; here is a simple Java example:
FROM eclipse-temurin:17-jre
WORKDIR /app
# copy your built artifact into image (adjust names/paths for your app)
COPY target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","/app/app.jar"]
